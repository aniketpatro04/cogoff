services:

  api:

    build: .

    container_name: cognitive-offloader-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./logs:/app/logs

    restart: unless-stopped